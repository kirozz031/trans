generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./db.sqlite"
}

model User {
  id           Int      @id @default(autoincrement())
  email        String   @unique
  password     String
  displayName  String   @unique
  avatar       String?
  createdAt    DateTime @default(now())

  // === NEW: 2FA ===
  is2FAEnabled  Boolean   @default(false)
  totpSecret    String?   // null until verified

  // === NEW: GDPR (soft-delete + anonymization) ===
  deletedAt     DateTime?
  anonymizedAt  DateTime?

  friends      Friend[] @relation("UserFriends")
  friendOf     Friend[] @relation("FriendRelation")
  player1Matches Match[] @relation("Player1")
  player2Matches Match[] @relation("Player2")
}

model Friend {
  id        Int      @id @default(autoincrement())
  userId    Int
  friendId  Int
  status    String   @default("PENDING") // "PENDING" ou "ACCEPTED"
  createdAt DateTime @default(now())

  user   User @relation("UserFriends", fields: [userId], references: [id])
  friend User @relation("FriendRelation", fields: [friendId], references: [id])

  @@unique([userId, friendId])
}

model Match {
  id          Int      @id @default(autoincrement())
  player1Id   Int
  player2Id   Int
  player1Score Int
  player2Score Int
  winnerId    Int
  matchType   String   @default("NORMAL") // "NORMAL", "TOURNAMENT_SEMI", "TOURNAMENT_FINAL"
  createdAt   DateTime @default(now())

  player1 User @relation("Player1", fields: [player1Id], references: [id])
  player2 User @relation("Player2", fields: [player2Id], references: [id])
}